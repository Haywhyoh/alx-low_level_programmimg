#include <stdio.h>
/**
 * main - Prints alhabets 
 * in lower case
 *
 * Returns: 0 if code runs succesfuly
 */
int main(void)
{
	char alphabet [27];

	alphabet = "abcdefghijklmnopqrstuvwxyz";
	putchar(alphabet);
	return (0);
}
