#include "main.h"
/**
 * main - prints alphabet in
 * lower case
 *
 * Return: 0
 */
void print_alphabet(void)
}
	char k;

	for (k = 'a'; k <= 'z'; k++)
	{
		_putchar(k);
	}
	_putchar('\n');
	return (0);
}
